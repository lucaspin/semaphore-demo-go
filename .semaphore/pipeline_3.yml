version: v1.0
name: 'Pipeline 3 - ${{parameters.REF_TYPE}} - ${{parameters.REF}} - ${{parameters.REPOSITORY}}'
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Deploy 3
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo $REF_TYPE
            - echo $REF
            - echo $REPOSITORY
