version: v1.0
name: 'Deploy - ${{parameters.ENVIRONMENT}} - ${{parameters.TAG}}'
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Deploy to ${{parameters.ENVIRONMENT}}'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo "Starting deployment of $TAG to $ENVIRONMENT..."
            - sleep 5
            - echo "Done."
