version: v1.0
name: Semaphore Go CI example
agent:
  machine:
    type: s1-lucaspin-test-3
    os_image: ubuntu1804
blocks:
  - name: Build project
    task:
      jobs:
        - name: go get & build
          commands:
            - 'echo "hello, darkness, my old friend"'
            - 'for ((i=1; i<=100; i++)); { echo $i; sleep 0.2; }'
