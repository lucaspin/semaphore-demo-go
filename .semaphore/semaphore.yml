version: v1.0
name: Semaphore Go CI example
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2204
blocks:
  - name: Build project
    task:
      jobs:
        - name: go get & build
          commands:
            - echo $SEMAPHORE_CACHE_S3_URL
            - echo $SEMAPHORE_CACHE_S3_BUCKET
      secrets:
        - name: semaphore-new-cache-credentials
