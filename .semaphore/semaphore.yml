version: v1.0
name: Semaphore Go CI example
agent:
  machine:
    type: s1-test-deploy
    os_image: ubuntu1804
blocks:
  - name: Build project
    task:
      jobs:
        - name: go get & build
          commands:
            - 'echo "hello, darkness, my old friend"'
            - 'for ((i=1; i<=1000; i++)); { echo $i; sleep 0.2; }'
