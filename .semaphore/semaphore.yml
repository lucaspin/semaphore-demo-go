version: v1.0
name: Semaphore Go CI example
agent:
  machine:
    type: s1-test-deploy
    os_image: ubuntu1804
blocks:
  - name: Build project
    task:
      jobs:
        - name: go get & build
          commands:
            - 'echo "hello, darkness, my old friend"'
